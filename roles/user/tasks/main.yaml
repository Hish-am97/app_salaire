
    - name: ajout group
      group:
       name: exam
       state: present

    - name: create user
      user: 
        name: web.hicham.form
        group: exam
        state: present

    - name: create user
      user: 
        name: db.hicham.form
        group: exam
        state: present
        
    - name: add clef ssh
      authorized_key:
        user: web.hicham.form
        state: present
        key: "{{ lookup('file', '/root/.ssh/id_rsa.pub') }}"
        
    - name: add clef ssh
      authorized_key:
        user: db.hicham.form
        state: present
        key: "{{ lookup('file', '/root/.ssh/id_rsa.pub') }}"
        
    - name: Ajout de du user dans sudo
      template:
        src: "/var/jenkins_home/workspace/Examapp-salaireTest/roles/user/tasks/templates/sudoweb.j2"
        dest: "/etc/sudoers.d/web.hicham.form"
    - name: Ajout de du user dans sudo
      template:
        src: "/var/jenkins_home/workspace/Examapp-salaireTest/roles/user/tasks/templates/sudodb.j2"
        dest: /etc/sudoers.d/db.hicham.form
