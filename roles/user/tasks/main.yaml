
    - name: ajout group
      group:
       name: exam
       state: present

    - name: create user
      user: web.hicham.form
        name: 
        group: exam
        state: present

    - name: add clef ssh
      authorized_key:
        user: web.hicham.form
        state: present
        key: "{{ lookup('file', '/home/web.hicham.form/.ssh/id_rsa.pub') }}"

    - name: Ajout de user dans sudo
      template:
        src: sudo.j2
        dest: /etc/sudoers.d/web.hicham.form
